# 安全政策

## 支持范围

| 插件版本 | OMP 版本 | 状态 |
|---|---|---|
| 0.1.x | 18.0.4 | 当前验证基线 |

同一主版本的后续 OMP 版本会执行结构预检，但在完成 CI 和真实 `/settings` 验证前不视为正式支持。OMP 19 及以上版本默认失败关闭。

## 私密报告

可能导致凭据或配置值泄露、错误修改用户配置、阻止 OMP 启动、执行非预期代码或绕过失败关闭的问题，请通过本仓库 GitHub Security 页面提交私密漏洞报告，不要先创建公开 Issue。

报告请包含：

- OMP 和插件版本；
- 安装方式与操作系统；
- 最小复现步骤；
- 兼容提示或已脱敏日志；
- 是否仍能打开官方英文 `/settings`；
- 影响范围和你已验证的缓解方式。

不要提交完整 `config.yml`、API Key、token、会话内容或日志目录。只提供复现所需的脱敏最小片段。

普通兼容性、误译和显示问题请使用公开 Issue 模板。
