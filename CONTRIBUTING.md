# 贡献指南

感谢你帮助 OMP 的设置界面变得更易理解。

## 当前阶段

`0.1.0` 已实现 OMP 18.0.4 的 Extension 入口、完整翻译、兼容预检、原子应用与回滚、覆盖/漂移检查和真实 TUI 验证。开始贡献前请阅读：

- [产品需求](docs/PRD.md)
- [技术设计](docs/TECHNICAL-DESIGN.md)
- [翻译规范](docs/TRANSLATION-GUIDE.md)

任何实现都必须遵守“不分叉 OMP、不复制设置面板、不改变设置语义”的核心边界。

## 可以贡献什么

- 修正或补充设置翻译；
- 复核风险说明和技术术语；
- 适配新的官方 OMP 版本；
- 改进覆盖率和兼容检查；
- 补充单元、契约或真实 TUI 验证；
- 提议并推动 OMP 上游本地化 API；
- 修正文档中的事实错误。

以下内容不在项目范围：

- OMP 全局汉化；
- 修改模型提示词或默认回复语言；
- 注册中文命令别名；
- 重打包官方二进制；
- 引入在线翻译、遥测或用户配置采集；
- 为追求“全中文”而翻译设置键、枚举值、模型名或工具名。

## 开发环境要求

最低开发环境：

- macOS 或 Linux；
- Bun 1.4.0 或更新版本；
- 官方 OMP 18.0.4 或项目兼容矩阵中列出的版本；
- Git。

项目保持以下脚本契约：

- `bun test`：单元和契约测试；
- `bun run typecheck`：TypeScript 类型检查；
- `bun run coverage:check`：翻译覆盖检查；
- `bun run drift:check`：上游 Schema 漂移检查；
- `bun run smoke`：插件加载冒烟检查，不代替真实交互式 TUI 验证；
- `bun run check`：执行全部自动检查。

## 翻译贡献流程

1. 确认设置路径和选项值存在于目标 OMP Schema。
2. 只读取锁定版本的 OMP 官方英文说明和相关源码，理解实际行为；不输入或复用第三方中文译文。
3. 使用本项目自己的 LLM 能力生成初稿，按克制翻译原则保留产品名和技术名词。
4. 按 `docs/TRANSLATION-GUIDE.md` 逐条复核翻译数据。
5. 更新对应英文原文哈希。
6. 运行 `bun run coverage:check` 和 `bun run drift:check`。
7. 在 PR 中列出涉及的设置路径和关键术语决策。

翻译 PR 不应夹带运行逻辑重构。

## 代码贡献原则

- OMP 内部导入只能出现在宿主适配层；
- 翻译数据不得包含执行逻辑；
- 兼容检查必须先于任何宿主对象写入；
- 写入失败必须完整回滚；
- 新增设置默认英文回退；
- 不读取当前设置值；
- 不增加运行时网络请求；
- 不用 `try/catch` 静默吞掉不兼容错误；
- 不针对单个 OMP 补丁版本散落条件分支。

## 测试要求

修复或功能 PR 应覆盖可观察行为，至少包括与改动直接相关的：

- 成功应用翻译；
- 英文回退；
- 幂等性；
- 结构不兼容失败关闭；
- 中途失败完整回滚；
- 上游 Schema 漂移报告。

影响设置面板的改动还必须在真实 OMP 中打开 `/settings` 验证。只测试翻译对象或源码文本不能代替真实界面验证。

## Pull Request 要求

PR 描述应包含：

- 问题与用户影响；
- 采用的解决方式；
- 影响的 OMP 版本；
- 影响的设置路径；
- 已运行的检查及其结果；
- 兼容风险与英文回退行为；
- 如涉及 UI，真实 `/settings` 验证结果。

保持提交聚焦。翻译、适配器重构和发布基础设施尽量拆分为独立 PR。

## Commit 建议

推荐使用清晰的 Conventional Commit 前缀：

- `feat:` 新能力；
- `fix:` 行为修复；
- `i18n:` 翻译新增或修正；
- `compat:` OMP 版本适配；
- `test:` 测试；
- `docs:` 文档；
- `chore:` 工程维护。

## 安全问题

如果问题可能导致读取或泄露凭据、修改错误配置、阻止 OMP 启动或执行非预期代码，请不要先公开 Issue；请使用 GitHub Security 的私密漏洞报告渠道。

## 许可证

提交贡献即表示你有权提供相关内容，并同意贡献按本项目 [MIT License](LICENSE) 发布。活动翻译必须从官方英文原文独立生成；不接受复制、改写或批量导入第三方现成译文。
